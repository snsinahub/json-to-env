name: JSON TO ENV
description: Read JSON file and export as Actions environment variables
inputs:
  json_path:
    required: true
    default: ''

outputs:
  envs:
    description: environment variables

runs:
  using: "node20"
  main: src/index.js